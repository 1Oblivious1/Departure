### **Личный дневник (DevOps-инженер / Системный архитектор проекта "Птичка")**

---

### Я решил погрузиться в мир разработки, начиная с основ баз данных и алгоритмов. Моя цель — не просто освоить технологии, но и понять, как они складываются в работающие системы, как проектировать их эффективно для проекта и как решать возникающие проблемы с точки зрения DevOps и архитектуры. Этот дневник — моя попытка зафиксировать каждый шаг, каждое открытие и вывод на этом пути, чтобы видеть свой прогресс и лучше усваивать новый материал, напрямую применимый к нашему проекту.

---

## **07.02.2025**

🔹 **Фокус:** Анализ и проектирование баз данных.
- Проанализировал требования к базе данных для выявления сущностей, их атрибутов и взаимосвязей.
- Освоил процесс перевода текстового описания в структуру базы данных.
- Определил таблицы для основных сущностей: **User**, **Task**, **Friendship**, **Photo**, **Location**, **Achievement**.

🚀 **Вывод:** Это фундаментальный этап. Для проекта правильное определение сущностей (`User`, `Task`, `Photo`, `Location`) и их связей обеспечит надежную основу для хранения данных о пользователях, их заданиях (сфотографировать себя в местах) и фотографиях. Это упростит разработку API бэкендом и обеспечит масштабируемость системы. Как DevOps, я смогу эффективнее спланировать развертывание и обслуживание этой БД.

---

## **08.02.2025**

🔹 **Фокус:** Работа с PostgreSQL: создание таблиц и типы данных.
- Научился создавать таблицы в PostgreSQL с использованием команды `CREATE TABLE`.
- Разобрался с основными типами данных: `SERIAL` (для ID), `VARCHAR`, `TEXT` (для описаний), `INT`, `DECIMAL` (для координат, если не используется PostGIS), `BYTEA` (для изображений, если решим хранить их в БД, а не в S3-совместимом хранилище), `TIMESTAMP` (для дедлайнов).
- Добавил ограничения для обеспечения целостности данных: `PRIMARY KEY`, `FOREIGN KEY`, `CHECK`.

🚀 **Вывод:** Знание синтаксиса `CREATE TABLE` и типов данных PostgreSQL позволит мне как архитектору точно определить структуру таблиц для `User`, `Task` и др. в проекте, а как DevOps – корректно развернуть и настроить базу данных PostgreSQL (выбранную для проекта 11.03), обеспечив целостность данных о заданиях, пользователях и их фото.

---

## **09.02.2025**

🔹 **Фокус:** Реализация связей между таблицами в PostgreSQL.
- Освоил три типа связей: "Один ко многим" (например, `"User"` ↔ `"Task"` – один пользователь может выполнить много заданий), "Многие ко многим" (реализовано через промежуточную таблицу, например, `"User_Achievement"`), и взаимную исключительность (в `"Photo"`: `user_id` для фото профиля, `task_id` для фото по заданию).
- Научился использовать внешние ключи для поддержания ссылочной целостности.

🚀 **Вывод:** Понимание типов связей критично для проекта. Например, связь "один ко многим" между `User` и `Task` (если задание уникально для пользователя) или "многие ко многим" для общих заданий, должна быть корректно реализована через внешние ключи. Это обеспечит ссылочную целостность и упростит сложные запросы для бэкенда на ASP.NET Core, а мне, как DevOps, облегчит миграции и обслуживание БД.

---

## **10.02.2025**

🔹 **Фокус:** Заполнение таблиц данными в PostgreSQL.
- Освоил команду `INSERT INTO` для добавления данных в таблицы.
- Разобрался, как соблюдать ограничения при вставке данных (уникальность, проверочные условия).
- Научился работать с шестнадцатеричным форматом для хранения изображений в поле `BYTEA`.

🚀 **Вывод:** Умение работать с `INSERT INTO` и понимать ограничения важно для подготовки тестовых данных и скриптов миграции. Знание формата `BYTEA` пригодится, если первоначальная архитектура будет предполагать хранение фото прямо в БД, хотя для DevOps это обычно сигнал к рассмотрению объектных хранилищ для масштабируемости.

---

## **11.02.2025**

🔹 **Фокус:** Написание SQL-запросов для выборки данных.
- Научился писать запросы с использованием `SELECT` для получения данных из одной или нескольких таблиц.
- Применил `JOIN` для объединения данных из связанных таблиц (например, пользователь и его выполненные задания).
- Освоил фильтрацию данных с помощью `WHERE` (например, задания в определенной локации).
- Разобрался с агрегирующими функциями и операторами, такими как `DISTINCT`.

🚀 **Вывод:** Эти навыки позволят мне как архитектору проектировать эффективные API, которые будут запрашивать данные (например, список заданий для пользователя с учетом его геолокации). Как DevOps, я смогу анализировать медленные запросы и помогать бэкенд-команде их оптимизировать, а также готовить скрипты для отчетности или обслуживания.

---

## **12.02.2025**

🔹 **Фокус:** Удаление таблиц в PostgreSQL.
- Освоил команду `DROP TABLE IF EXISTS` для безопасного удаления таблиц.
- Разобрался с порядком удаления таблиц, чтобы избежать ошибок, связанных с зависимостями (внешними ключами).

🚀 **Вывод:** Навык безопасного удаления таблиц важен для DevOps при разработке скриптов очистки тестовых окружений или при откате миграций БД для "Птички", гарантируя, что данные не будут повреждены из-за нарушения зависимостей.

---

## **13.02.2025**

🔹 **Фокус:** Введение в алгоритмы по книге "Грокаем алгоритмы".
- Прочитал первую главу книги Адитьи Бхаргавы.
- Изучил основные концепции: что такое алгоритм, его важность.
- Рассмотрел примеры простых алгоритмов, таких как бинарный поиск.
- Понял, как алгоритмы помогают решать задачи эффективно.

🚀 **Вывод:** Понимание основ алгоритмов поможет мне как архитектору оценивать сложность предлагаемых решений для проекта (например, алгоритма подбора персонализированных заданий, упомянутого 02.02). Как DevOps, я буду лучше понимать, почему некоторые операции могут быть ресурсоемкими и как их можно оптимизировать на уровне инфраструктуры или конфигурации.

---

## **14.02 – 15.02.2025**

🔹 **Фокус:** Изучение алгоритма бинарного поиска.
- Изучил, как работает бинарный поиск.
- Разобрал реализацию бинарного поиска на псевдокоде.
- Сравнил бинарный поиск с линейным поиском по эффективности.
- Понял ключевое условие для бинарного поиска: данные должны быть отсортированы.

🚀 **Вывод:** Хотя прямое применение бинарного поиска в проекте может быть не везде очевидно на старте (например, поиск заданий по названию, если они отсортированы), это знание подчеркивает важность выбора правильных структур данных и индексов в PostgreSQL для бэкенда, чтобы обеспечить быструю выборку, например, ближайших заданий, что снизит нагрузку на сервер.

---

## **16.02 – 18.02.2025**

🔹 **Фокус:** Сложность алгоритмов и нотация Big O.
- Разобрался в оценке сложности алгоритмов с помощью нотации **Big O**: O(1), O(log n), O(n), O(n^2).
- Научился анализировать, как выбор алгоритма влияет на производительность программы.
- Научился считать сложность алгоритма в представлении нотации Big O.

🚀 **Вывод:** Оценка сложности алгоритмов через Big O – ключевой навык для архитектора "Птички". Это позволит выбирать оптимальные структуры данных и алгоритмы для обработки запросов (например, выборка заданий поблизости, упомянутая 05.02, или алгоритм создания заданий, упомянутый 23.02-24.02), обеспечивая масштабируемость и производительность системы, что напрямую влияет на стабильность и ресурсы, управляемые DevOps.

---

## **19.02 – 22.02.2025**

🚀 **Олимпиада** – работа над проектом не велась.

---

## **23.02.2025**

🚀 Была проведена оценка достигнутых результатов в команде "Птичка".

---

## **24.02.2025**

🔹 **Фокус:** Оптимизация SQL-запросов.
- Изучил способы оптимизации SQL-запросов: использование индексов (`CREATE INDEX`), анализ плана выполнения (`EXPLAIN`), оптимизация `JOIN`.
- Применил индексы к таблицам с частыми запросами.

🚀 **Вывод:** Оптимизация SQL-запросов и использование индексов будут критически важны для проекта, особенно для быстрых выборок заданий по геолокации или фильтрации фото. Как архитектор, я смогу проектировать эффективные запросы для API, а как DevOps – применять и мониторить индексы в PostgreSQL для поддержания высокой производительности, что подтвердилось как необходимая задача (16.05-17.05).

---

## **25.02.2025**

🔹 **Фокус:** Триггеры и хранимые процедуры в PostgreSQL.
- Освоил триггеры: создал простой триггер для автоматической записи времени последнего обновления.
- Изучил хранимые процедуры (`CREATE FUNCTION`) для инкапсуляции логики на стороне БД.
- Реализовал процедуру для автоматического создания новой задачи после достижения пользователем определенного уровня активности.

🚀 **Вывод:** Для "Птички" триггеры могут использоваться для обновления `user.level_activity` или `achievement.date_received`. Хранимые процедуры могут инкапсулировать сложную логику, например, генерацию персонализированных заданий. Как архитектор, я могу предложить такие решения для оптимизации, а как DevOps – должен буду обеспечить их корректное развертывание и версионирование вместе с остальной схемой БД.

---

## **26.02.2025**

🔹 **Фокус:** Введение в сортировку и алгоритмы сортировки.
- Начал изучение алгоритмов сортировки: пузырьковая сортировка (`O(n^2)`) и быстрая сортировка (`O(n log n)`).
- Сравнил эффективность разных алгоритмов и понял, что выбор метода сортировки зависит от размера данных.

🚀 **Вывод:** Понимание алгоритмов сортировки важно для архитектора при проектировании API, которые возвращают отсортированные данные (например, список пользователей по уровню активности, список задач по дедлайну). Это поможет выбрать, где выполнять сортировку (в БД или в приложении), что повлияет на производительность и нагрузку на компоненты системы, управляемые DevOps.

---

## **27.02.2025**

🔹 **Фокус:** Реализация алгоритмов сортировки на псевдокоде.
- Написал на псевдокоде алгоритмы сортировки: **Bubble Sort** и **Quick Sort**.
- Провел тестирование на небольших наборах данных и визуализировал шаги работы алгоритмов.

🚀 **Вывод:** Практическая реализация алгоритмов, даже на псевдокоде, укрепляет понимание их работы. Это позволит мне как архитектору более обоснованно обсуждать с backend-разработчиками выбор методов сортировки и обработки данных для API, например, для ленты новостей или списка заданий.

---

## **28.02.2025**

🔹 **Фокус:** Сравнение линейного и бинарного поиска.
- Сравнил линейный и бинарный поиск в реальных условиях: провел замеры времени выполнения.
- Ещё раз убедился, насколько критично заранее отсортировать данные для бинарного поиска.
- Попробовал оптимизировать простой поиск, сокращая ненужные итерации.

🚀 **Вывод:** Это знание подкрепляет необходимость правильного проектирования индексов в PostgreSQL. Если данные для поиска (например, пользователи по ID, задачи по уникальному коду) могут быть эффективно проиндексированы, СУБД сама сможет использовать оптимизированные методы поиска, что снизит нагрузку и улучшит время отклика API.

---

## **01.03.2025**

🔹 **Фокус:** Изучение основ реляционной алгебры.
- Изучил операции объединения (`UNION`), пересечения (`INTERSECT`) и разности (`EXCEPT`).
- Разобрал картезианское произведение и его связь с SQL-запросами.

🚀 **Вывод:** Понимание реляционной алгебры поможет мне как архитектору более точно формулировать сложные запросы к БД (например, для аналитики или формирования сложных выборок для пользователей) и понимать, как SQL-операторы транслируются в операции над множествами, что важно для оптимизации.

---

## **02.03.2025**

🔹 **Фокус:** Курсоры в PostgreSQL и работа с большими данными.
- Освоил курсоры в PostgreSQL для обработки больших наборов данных.
- Изучил, как с помощью курсоров поэтапно извлекать записи, не загружая все данные в память.
- Написал простой цикл с курсором для обработки данных в таблице **Task**.

🚀 **Вывод:** Если в проекте возникнет необходимость обрабатывать очень большие объемы данных (например, массовая рассылка уведомлений всем пользователям или пересчет рейтингов), курсоры могут стать решением для бэкенда. Как DevOps, я должен знать об этой возможности для планирования ресурсов и мониторинга таких операций.

---

## **03.03 – 04.03.2025**

🔹 **Фокус:** Основы систем обработки данных (data-intensive systems).
- Узнал, что системы, работающие с большими объемами данных, называются *data-intensive*.
- Понял основные задачи таких систем: хранение, обработка и передача данных.
- Осознал важность выбора подходящей архитектуры для конкретной задачи.

🚀 **Вывод:** Проект, с его пользовательскими данными, фото, геолокациями и заданиями, имеет потенциал стать data-intensive. Это знание подчеркивает важность выбора правильной архитектуры (например, трехзвенной, как решено 11.02), масштабируемой БД (PostgreSQL) и эффективных API. Как архитектор, я должен учитывать это при проектировании, а как DevOps – при планировании инфраструктуры.

---

## **05.03 – 06.03.2025**

🔹 **Фокус:** Модели данных и запросов (SQL vs NoSQL).
- Изучил различия между реляционными (SQL) и документными (NoSQL) базами данных.
- Понял, что реляционные БД используют строгие схемы, а NoSQL — более гибкие модели.
- Узнал о графовых базах данных и их применимости для взаимосвязанных данных (например, социальные связи `Friendship`).

🚀 **Вывод:** Хотя для проекта выбрана PostgreSQL (SQL), понимание NoSQL важно. Например, для хранения пользовательских сессий или кэширования можно рассмотреть Redis (NoSQL). Для социальных связей (`Friendship`) графовая БД могла бы быть эффективнее, но для текущего масштаба PostgreSQL справится. Это знание позволяет мне как архитектору делать более информированный выбор компонентов системы.

---

## **07.03 – 08.03.2025**

🔹 **Фокус:** Хранение и извлечение данных (B-деревья, LSM-деревья, индексы).
- Узнал, как устроены B-деревья (PostgreSQL использует их для индексов) и LSM-деревья.
- Понял, что индексы ускоряют доступ к данным, но требуют дополнительных ресурсов.

🚀 **Вывод:** Это углубляет мое понимание работы индексов в PostgreSQL, которые критически важны для производительности (особенно для запросов по геолокации `Location` или поиска `Task`). Как архитектор, я могу более точно проектировать схемы и индексы, а как DevOps – понимать влияние индексов на использование диска и производительность операций записи.

---

## **09.03 – 10.03.2025**

🔹 **Фокус:** Кодирование и эволюция схем данных.
- Осознал важность правильного кодирования данных для минимизации занимаемого места.
- Изучил популярные форматы сериализации: JSON и Protocol Buffers (для API).
- Понял, что эволюция схем данных (миграции БД) должна быть обратно совместимой.

🚀 **Вывод:** Для API (между React-фронтендом и ASP.NET Core-бэкендом) будет использоваться JSON. Понимание эволюции схем критично для DevOps при управлении миграциями БД PostgreSQL с помощью Entity Framework Core, обеспечивая плавные обновления без даунтайма.

---

## **11.03 – 13.03.2025**

🔹 **Фокус:** Репликация.
- Узнал, что репликация позволяет создавать копии данных для повышения отказоустойчивости.
- Синхронная репликация гарантирует согласованность, но может замедлить запись.
- Асинхронная репликация быстрее, но допускает задержки.

🚀 **Вывод:** Знание репликации позволит мне, как архитектору, спроектировать отказоустойчивую БД PostgreSQL, обеспечив доступность данных о пользователях и их прогрессе даже при сбоях. Как DevOps, я смогу настроить и поддерживать репликацию, минимизируя время простоя и риск потери данных, особенно когда проект выйдет в прод.

---

## **14.03 – 16.03.2025**

🔹 **Фокус:** Партиционирование.
- Узнал, что партиционирование (sharding) разделяет данные между несколькими серверами.
- Можно партиционировать по ключу (например, ID пользователя) или по диапазону значений.
- Проблемы: маршрутизация запросов и выполнение операций над несколькими партициями.

🚀 **Вывод:** По мере роста пользовательской базы и объема данных (особенно таблицы `Photo` или `Task`), партиционирование PostgreSQL может стать необходимым для масштабирования. Как архитектор, я должен предвидеть такую потребность, а как DevOps – быть готовым к реализации и управлению партиционированной БД.

---

## **17.03 – 20.03.2025**

🔹 **Фокус:** Транзакции.
- Узнал: ACID-свойства транзакций обеспечивают согласованность данных.
- Изоляция транзакций может быть реализована на разных уровнях (например, Read Committed).
- CAP-теорема ограничивает одновременное достижение согласованности, доступности и устойчивости к разделению.

🚀 **Вывод:** Для проекта ACID-транзакции в PostgreSQL важны для операций типа регистрации пользователя (`User`), создания задачи (`Task`), загрузки фото (`Photo`). Как архитектор, я должен убедиться, что бэкенд на ASP.NET Core корректно использует транзакции. Понимание CAP-теоремы поможет при проектировании распределенных аспектов системы в будущем.

---

## **21.03 – 22.03.2025**

🔹 **Фокус:** Обработка потоков данных.
- Узнал: Потоки данных (streams) позволяют обрабатывать события в реальном времени.
- Apache Kafka — популярная платформа для работы с потоками данных.
- Потоки могут использоваться для аналитики, уведомлений и синхронизации данных.

🚀 **Вывод для проекта 'Птичка':** В "Птичке" потоковая обработка может пригодиться для системы уведомлений о новых заданиях (упомянуто 05.02) или для аналитики действий пользователей в реальном времени. Как архитектор, я могу рассмотреть Kafka или RabbitMQ для таких задач, а как DevOps – буду отвечать за развертывание и поддержку этой части инфраструктуры.

---

## **22.03 – 23.03.2025**

🔹 **Фокус:** Распределенные системы.
- Узнал: Распределенные системы сталкиваются с проблемами сетевых задержек и сбоев.
- Алгоритмы консенсуса (например, Raft) помогают решать проблемы координации.
- CAP-теорема определяет компромиссы между согласованностью, доступностью и устойчивостью.

🚀 **Вывод:** По мере роста роста и возможного разделения на микросервисы, понимание проблем распределенных систем станет критичным. Как архитектор, я должен буду проектировать сервисы с учетом этих проблем, а как DevOps – настраивать сетевое взаимодействие, балансировку нагрузки и мониторинг для таких систем.

---

## **24.03 – 25.03.2025**

🔹 **Фокус:** Производительность и масштабируемость.
- Узнал: Масштабирование можно выполнять горизонтально (добавление узлов) или вертикально (усиление оборудования).
- Горизонтальное масштабирование сложнее, но более гибкое.
- Оптимизация запросов и индексов критически важна для производительности.

🚀 **Вывод:** Для проекта (веб-приложение на React и ASP.NET Core) горизонтальное масштабирование бэкенд-сервисов и, возможно, БД PostgreSQL будет ключевым по мере роста нагрузки. Как архитектор, я должен проектировать stateless-сервисы, а как DevOps – настраивать балансировщики нагрузки и автомасштабирование (например, в Kubernetes).

---

## **26.03 – 27.03.2025**

🔹 **Фокус:** Безопасность и мониторинг.
- Узнал: Важность шифрования данных и управления доступом.
- Мониторинг помогает обнаруживать проблемы до их эскалации.
- Логирование и трассировка запросов необходимы для отладки.

🚀 **Вывод:** Безопасность (шифрование паролей, защита API) и мониторинг (логов PostgreSQL, ASP.NET Core, React-приложения) – критически важные аспекты. Как архитектор, я должен заложить основы безопасного дизайна. Как DevOps, я буду настраивать системы логирования (ELK, Grafana Loki), мониторинга (Prometheus, Grafana) и обеспечивать безопасность инфраструктуры.

---

## **28.03 – 29.03.2025**

🔹 **Фокус:** Заключение: Итоги книги.
- Узнал: Проектирование data-intensive приложений требует баланса между производительностью, надежностью и масштабируемостью.
- Важно выбирать технологии, соответствующие требованиям системы.
- CAP-теорема и принципы распределенных систем помогают принимать осознанные решения.

🚀 **Вывод:** Все эти принципы напрямую применимы к проекту. Выбор стека (React, ASP.NET Core, PostgreSQL) был сделан с учетом этих факторов. Моя роль как архитектора и DevOps – обеспечить, чтобы этот баланс соблюдался на всех этапах жизненного цикла проекта, от проектирования до эксплуатации.

---

## **30.03 – 31.03.2025**

🔹 **Фокус:** Анализ процесса настройки подключений.
- Узнал: Важность начального минимализма: сначала реализовать базовую функциональность, чтобы убедиться, что все компоненты работают вместе.
- Отладка взаимодействия между фронтендом, бэкендом и базой данных требует значительных временных затрат.
- Необходимость внимательного контроля за портами подключения для избежания конфликтов и ошибок.

🚀 **Вывод:** Этот опыт подтверждает важность четкого определения сетевых взаимодействий и конфигураций для проекта, особенно при использовании Docker (как решено 18.04-19.04). Как DevOps, я должен обеспечить правильную настройку `docker-compose.yml` или Kubernetes-манифестов, чтобы фронтенд (React), бэкенд (ASP.NET Core) и БД (PostgreSQL) могли корректно общаться.

---

## **01.04.2025**

🔹 **Фокус:** Введение в книгу "Банда четырёх".
- Узнал: Книга *"Приемы объектно-ориентированного проектирования: Паттерны проектирования"* от Эриха Гамма и других авторов — это классическое руководство, которое описывает 23 паттерна.
- Паттерны делятся на три категории: порождающие, структурные и поведенческие.
- Цель паттернов — упростить разработку, сделать код более гибким, поддерживаемым и масштабируемым.

🚀 **Вывод для проекта 'Птичка':** Знание паттернов проектирования поможет мне как архитектору проектировать бэкенд "Птички" на ASP.NET Core более качественно. Это приведет к созданию более поддерживаемого и расширяемого кода, что косвенно облегчит работу DevOps по развертыванию и обновлению системы.

---

## **01.04.2025 - 02.04.2025**

🔹 **Фокус:** Singleton.
- Узнал: Singleton — это порождающий паттерн, который гарантирует, что у класса есть только один экземпляр, и предоставляет глобальную точку доступа к нему.
- Примеры использования: логгеры, конфигурации приложений, менеджеры соединений с базой данных.
- Возможные проблемы: злоупотребление Singleton может привести к трудностям в тестировании и чрезмерной зависимости компонентов друг от друга.

🚀 **Вывод:** В бэкенде Singleton может использоваться для сервиса конфигурации или для управления пулом соединений к PostgreSQL (хотя EF Core этим управляет). Как архитектор, я должен взвесить его применение, а как DevOps – понимать, как такие компоненты влияют на управление состоянием и ресурсами в контейнерах.

---

## **03.04.2025 - 05.04.2025**

🔹 **Фокус:** Factory Method.
- Узнал: Factory Method — это порождающий паттерн, который определяет общий интерфейс для создания объектов, позволяя подклассам изменять тип создаваемых объектов.
- Пример: фреймворк, который позволяет пользователям добавлять собственные реализации через наследование.
- Преимущества: делегирование создания объектов подклассам делает систему более гибкой.

🚀 **Вывод:** В "Птичке" Factory Method мог бы использоваться, например, для создания разных типов `Task` или `Achievement` с разной логикой. Это сделает архитектуру бэкенда более гибкой и расширяемой, что упростит добавление нового функционала в будущем без больших переделок, что важно для долгосрочной поддержки DevOps.

---

## **06.04.2025 - 08.04.2025**

🔹 **Фокус:** Abstract Factory.
- Узнал: Abstract Factory — это порождающий паттерн, который предоставляет интерфейс для создания семейств связанных объектов без указания их конкретных классов.
- Пример: создание пользовательского интерфейса для разных операционных систем.
- Преимущества: упрощает работу с группами связанных объектов.

🚀 **Вывод для проекта 'Птичка':** Если бы веб-приложение поддерживало разные типы карт (Yandex Maps API, 2GIS API), Abstract Factory мог бы использоваться для создания семейства объектов, связанных с картами (маркеры, маршруты). Это упростило бы переключение или одновременную поддержку разных провайдеров карт в архитектуре.

---

## **09.04.2025 - 10.04.2025**

🔹 **Фокус:** Adapter.
- Узнал: Adapter — это структурный паттерн, который позволяет совместить несовместимые интерфейсы.
- Пример: адаптер для работы с устаревшим API или сторонними сервисами.
- Преимущества: позволяет повторно использовать существующий код без изменения его внутренней реализации.

🚀 **Вывод:** Паттерн Adapter будет полезен для интеграции с внешними API, например, Yandex Maps API. Если в будущем решим сменить провайдера карт, адаптер минимизирует изменения в основной логике бэкенда, что упростит обновление для DevOps.

---

## **11.04.2025 - 12.04.2025**

🔹 **Фокус:** Decorator.
- Узнал: Decorator — это структурный паттерн, который позволяет добавлять новую функциональность объекту, не изменяя его код.
- Пример: добавление дополнительных возможностей к компонентам пользовательского интерфейса.
- Преимущества: гибкость и возможность комбинировать разные декораторы.

🚀 **Вывод:** В проекте Decorator может использоваться на бэкенде для добавления функциональности к объектам `Task` или `User` (например, добавление временных бонусов к заданиям или специальных статусов пользователям) без изменения их основного класса. Это сохраняет гибкость архитектуры.

---

## **13.04.2025 - 14.04.2025**

🔹 **Фокус:** Применение Docker для запуска сервисов.
- Узнал: В статье Буренкова И.А. рассматривается Docker как инструмент виртуализации, позволяющий запускать приложения и сервисы в изолированных контейнерах .
- Docker работает напрямую с аппаратным обеспечением, что снижает расход системных ресурсов.
- Технология позволяет упаковывать все зависимости в образы, что упрощает развертывание и масштабирование.
- Автор показывает практические примеры использования Docker для развертывания сервисов.

🚀 **Вывод:** Docker – ключевая технология для проекта, как и было решено (18.04-19.04). Как архитектор, я смогу проектировать компоненты (бэкенд на ASP.NET Core, фронтенд на React, PostgreSQL) для упаковки в изолированные контейнеры. Как DevOps, я буду использовать Docker для создания образов, управления зависимостями (`Dockerfile`, `docker-compose.yml`) и развертывания всего стека приложения, что кардинально упростит CI/CD и обеспечит консистентность окружений.

---

## **15.04.2025 - 16.04.2025**

🔹 **Фокус:** Composite.
- Узнал: Composite — это структурный паттерн, который позволяет работать с группой объектов так же, как и с одиночным объектом.
- Пример: дерево файловой системы, где каждый узел может быть файлом или папкой.
- Преимущества: упрощает работу с древовидными структурами.

🚀 **Вывод:** В проекте паттерн Composite мог бы применяться, если бы у нас была сложная иерархическая структура заданий (например, мета-задания, состоящие из подзаданий) или категорий локаций. Это позволило бы единообразно обрабатывать как отдельные элементы, так и их группы в архитектуре бэкенда.

---

## **17.04.2025 - 19.04.2025**

🔹 **Фокус:** Observer.
- Узнал: Observer — это поведенческий паттерн, который создаёт механизм подписки, позволяя одним объектам следить за изменениями других.
- Пример: система уведомлений, где подписчики получают обновления о событиях.
- Преимущества: реализация слабой связи между объектами.

🚀 **Вывод:** Паттерн Observer идеально подойдет для системы уведомлений (например, о новых заданиях поблизости, упомянутых 05.02, или активностях друзей). Как архитектор, я смогу спроектировать слабосвязанную систему на бэкенде, где компоненты реагируют на события. Для DevOps это означает более предсказуемое поведение системы и возможность легко добавлять новые типы подписчиков/уведомлений без изменения ядра.

---

## **20.04.2025 - 22.04.2025**

🔹 **Фокус:** Command.
- Узнал: Command — это поведенческий паттерн, который инкапсулирует запрос как объект, что позволяет параметризовать клиентов различными запросами.
- Пример: реализация системы команд в текстовом редакторе (например, отмена/повтор действия).
- Преимущества: упрощение управления командами и их выполнение.

🚀 **Вывод:** В проекте паттерн Command может использоваться для обработки действий пользователя (например, "выполнить задание", "добавить друга", "загрузить фото"). Это позволит логировать действия, ставить их в очередь или реализовывать отмену. Такая архитектура бэкенда будет более гибкой и управляемой, что важно для DevOps при траблшутинге и анализе.

---

## **23.04.2025 - 25.04.2025**

🔹 **Фокус:** Strategy.
- Узнал: Strategy — это поведенческий паттерн, который определяет семейство алгоритмов, помещает каждый из них в отдельный класс и делает их взаимозаменяемыми.
- Пример: выбор стратегии сортировки или алгоритма маршрутизации.
- Преимущества: упрощение замены алгоритмов во время выполнения программы.

🚀 **Вывод:** Паттерн Strategy может быть полезен в для реализации различных алгоритмов подбора заданий для пользователей (например, "ближайшие", "самые популярные", "новые") или для разных способов расчета очков/рейтинга. Это позволит легко менять или добавлять стратегии на бэкенде без изменения основного кода, что упростит адаптацию продукта и его поддержку DevOps.

---

## **26.04.2025 – 30.04.2025**

🔹 **Фокус:** Углубленное изучение Docker и его экосистемы на основе статьи Selectel "Что такое Docker и как он работает".
- Детально разобрал, что Docker – это платформа, позволяющая упаковывать приложения и их зависимости в стандартизированные блоки, называемые **контейнерами**, для обеспечения их консистентной работы в различных окружениях.
- Укрепил понимание ключевых компонентов Docker:
    - **Образ (Image):** Шаблон только для чтения, содержащий инструкции для создания контейнера. Образы строятся на основе **Dockerfile** – текстового файла с командами. Образы могут наследоваться друг от друга, образуя слои, что оптимизирует хранение и передачу.
    - **Контейнер (Container):** Запущенный, исполняемый экземпляр образа. Контейнеры изолированы друг от друга и от хост-системы, но используют ядро хоста, что делает их легковесными.
    - **Docker Engine:** Клиент-серверное приложение, состоящее из демона Docker (серверной части), REST API для взаимодействия с демоном и CLI (клиентской части).
    - **Реестр (Registry):** Система хранения и распространения образов (например, Docker Hub – публичный реестр, или приватные, как Selectel Container Registry).
- Проанализировал основные преимущества Docker для разработки и эксплуатации:
    - **Воспроизводимость и консистентность окружений:** Устраняет проблему "у меня работает, а у тебя нет", гарантируя одинаковую среду от разработки до продакшена.
    - **Изоляция приложений:** Предотвращает конфликты зависимостей и обеспечивает безопасность.
    - **Эффективность использования ресурсов:** Контейнеры потребляют значительно меньше ресурсов по сравнению с виртуальными машинами, так как не требуют эмуляции целой ОС.
    - **Портативность:** Контейнеры можно легко переносить между различными машинами и облачными платформами.
    - **Быстрое развертывание и масштабирование:** Запуск и остановка контейнеров происходят почти мгновенно.
    - **Микросервисная архитектура:** Docker идеально подходит для построения и управления микросервисами.
- Изучил роль `docker-compose` как инструмента для определения и запуска многоконтейнерных приложений, где описываются сервисы, сети и тома в YAML-файле.

🚀 **Вывод:** Статья Selectel дала мне более глубокое и структурированное понимание Docker, что критически важно для успеха проекта.
- **Как системный архитектор:** Это знание укрепляет решение использовать Docker (принятое 18.04-19.04) для каждого компонента (React-фронтенд, ASP.NET Core-бэкенд, PostgreSQL). Я могу более точно проектировать `Dockerfile` для каждого сервиса, оптимизируя слои образов для скорости сборки и размера. Понимание изоляции и управления ресурсами поможет мне проектировать более отказоустойчивую и масштабируемую систему, особенно если проект будет развиваться в сторону микросервисов.
- **Как DevOps-инженер:** Глубокое понимание Docker Engine, образов, контейнеров и реестров позволит мне эффективно выстраивать CI/CD процессы. Я смогу автоматизировать сборку образов, их тестирование и деплой в различные окружения. Знание `docker-compose` (который уже используется) позволит мне лучше управлять локальной разработкой для команды и конфигурацией многоконтейнерного приложения при деплое (осуществленном 30.04-02.05). Это также поможет в мониторинге контейнеризированных приложений и более эффективном управлении ресурсами сервера.

---

## **30.04.2025 – 05.05.2025**

🔹 **Фокус:** Установка и использование Docker Desktop на Windows, на основе статьи Selectel "Как установить и использовать Docker Desktop на Windows".
- Разобрался с процессом установки Docker Desktop на Windows, включая необходимость активации WSL 2 (Windows Subsystem for Linux) для оптимальной работы и запуска Linux-контейнеров. Это ключевой момент, так как проект будет использовать Linux-образы для PostgreSQL и ASP.NET Core.
- Изучил интерфейс Docker Desktop: панель управления контейнерами, образами, томами (volumes) и сетями. Визуальный менеджмент значительно упрощает повседневные задачи, особенно для тех, кто только начинает работать с Docker или предпочитает GUI.
- Освоил запуск и управление контейнерами (например, тестового PostgreSQL или веб-сервера Nginx) через GUI Docker Desktop, а также убедился в возможности использования привычных команд Docker CLI через терминал, интегрированный с WSL 2.
- Глубже понял, как Docker Desktop облегчает локальную разработку для Windows-пользователей, предоставляя интегрированную среду для работы с Docker, которая благодаря WSL 2 очень близка к нативной Linux-среде. Это важно для консистентности окружений в команде.
- Узнал о дополнительных возможностях Docker Desktop, таких как управление расширениями (Extensions), которые могут добавить полезный функционал, и встроенная (хоть и опциональная) поддержка Kubernetes, что является хорошим заделом на будущее, даже если сейчас для проекта используется Docker Compose.

🚀 **Вывод для проекта 'Птичка':** Изучение Docker Desktop для Windows напрямую улучшает процесс разработки и тестирования для членов команды, работающих на Windows, и для меня как ответственного за DevOps-процессы.
- **Как системный архитектор:** Понимание особенностей Docker Desktop и его тесной интеграции с WSL 2 дает мне уверенность, что разработчики в команде, использующие Windows, смогут без особых проблем локально запускать и отлаживать всю связку сервисов (React-фронтенд, ASP.NET Core-бэкенд, PostgreSQL) с помощью единого `docker-compose.yml` файла, который был центральным элементом обсуждения (26.04-27.04). Это обеспечивает единообразие локальных окружений разработки, независимо от основной ОС разработчика, что критически важно для раннего выявления проблем интеграции компонентов и снижения расхождений между dev, staging и prod окружениями.
- **Как DevOps-инженер:** Глубокое знание Docker Desktop на Windows значительно упрощает мою задачу по поддержке команды. Я могу создавать более точные и полные инструкции по настройке локального окружения для Windows-пользователей, предвидя возможные проблемы с WSL 2 или виртуализацией. Это гарантирует, что образы, которые разработчики собирают или тестируют локально на своих Windows-машинах, будут вести себя предсказуемо в CI/CD пайплайнах и в конечном итоге в продакшене, который, скорее всего, будет работать на Linux-серверах. Удобный графический интерфейс Docker Desktop также помогает разработчикам быстрее осваивать Docker, самостоятельно управлять своими контейнерами и образами, а также эффективнее диагностировать проблемы, что снижает нагрузку на меня в плане базовой поддержки.

---

## **06.05.2025 – 10.05.2025**

🔹 **Фокус:** Запуск и управление контейнерами Docker с помощью команды `docker run` и связанных команд, на основе статьи Selectel "Запуск контейнера Docker".
- Детально изучил команду `docker run` – основной инструмент для запуска контейнеров. Разобрал наиболее важные флаги и их применение:
    - `-d` (detached mode): для запуска контейнеров в фоновом режиме, что необходимо для сервисов "Птички" (PostgreSQL, ASP.NET Core бэкенд).
    - `-p` или `--publish host_port:container_port`: для проброса портов из контейнера на хост-машину. Это позволит обращаться к API бэкенда, базе данных PostgreSQL и React-фронтенду.
    - `--name container_name`: для присвоения осмысленного имени контейнеру, что упрощает управление (например, `ptichka-db`, `ptichka-backend`).
    - `-e KEY=VALUE` или `--env KEY=VALUE`: для передачи переменных окружения внутрь контейнера. Критически важно для настройки PostgreSQL (пароли, пользователи, имя БД) и ASP.NET Core (строки подключения, конфигурационные параметры).
    - `-v host_path:container_path` или `--volume volume_name:container_path`: для монтирования томов (volumes) или директорий хоста (bind mounts). Абсолютно необходимо для сохранения данных PostgreSQL (`/var/lib/postgresql/data`) между перезапусками контейнера.
    - `--rm`: для автоматического удаления контейнера после его остановки, полезно для временных или тестовых запусков.
- Освоил команды для управления жизненным циклом контейнеров:
    - `docker ps [-a]`: для просмотра запущенных (и всех) контейнеров.
    - `docker stop <container_id_or_name>`: для остановки контейнера.
    - `docker start <container_id_or_name>`: для запуска остановленного контейнера.
    - `docker rm <container_id_or_name>`: для удаления контейнера.
    - `docker logs <container_id_or_name>`: для просмотра логов контейнера, что незаменимо при отладке.
    - `docker exec -it <container_id_or_name> <command>`: для выполнения команд внутри уже запущенного контейнера (например, `psql` для подключения к PostgreSQL или `bash` для доступа к файловой системе контейнера).
- Попрактиковался в запуске контейнеров для основных компонентов проекта "Птичка" (PostgreSQL, Nginx как пример веб-сервера для React-сборки, и гипотетического ASP.NET Core приложения), применяя изученные опции.

🚀 **Вывод для проекта 'Птичка':** Это практическое знание команд `docker run` и управления контейнерами является фундаментом для развертывания и эксплуатации "Птички".
- **Как системный архитектор:** Понимание опций `docker run` позволяет мне точно специфицировать, как каждый сервис (PostgreSQL, ASP.NET Core, React) должен быть запущен и сконфигурирован. Эти знания напрямую транслируются в создание корректных `Dockerfile` и, что более важно, в определение сервисов в `docker-compose.yml` (используемом для проекта, как обсуждалось 26.04-27.04 и реализовано 30.04-02.05). Я могу четко определить необходимые порты, переменные окружения для строк подключения, тома для персистентности данных PostgreSQL, что обеспечивает правильную и предсказуемую работу всей системы.
- **Как DevOps-инженер:** Владение этими командами критически важно для повседневной работы. Я могу вручную запускать и отлаживать отдельные контейнеры, проверять их конфигурацию и логи. Это незаменимо при настройке CI/CD пайплайнов, где могут потребоваться скрипты для запуска контейнеров (например, для интеграционных тестов или специфических задач развертывания). Понимание `docker logs` и `docker exec` ускоряет диагностику проблем. Кроме того, это позволяет мне эффективно помогать разработчикам с локальной настройкой и отладкой их контейнеризированных частей приложения. Эти навыки стали основой для успешного развертывания всего стека через `docker-compose` (30.04-02.05).

---

Хорошо, вот обновленная запись для дневника, без упоминания названия проекта, основанная на изучении статьи "Что такое Docker Compose и как им пользоваться" от Selectel:

---

## **11.05.2025 – 15.05.2025**

🔹 **Фокус:** Освоение Docker Compose для управления многоконтейнерными приложениями, на основе статьи Selectel "Что такое Docker Compose и как им пользоваться".
- Глубоко изучил Docker Compose как инструмент для определения и запуска приложений, состоящих из нескольких Docker-контейнеров, с помощью единого конфигурационного файла `docker-compose.yml`.
- Разобрал структуру и синтаксис YAML-файла `docker-compose.yml`:
    - `version`: для указания версии схемы Compose.
    - `services`: для определения каждого компонента приложения (например, `database_service`, `backend_service`, `frontend_service`).
        - `image` или `build`: для указания образа или пути к Dockerfile.
        - `ports`: для проброса портов.
        - `volumes`: для монтирования томов (особенно важно для персистентности данных базы данных).
        - `environment`: для установки переменных окружения (настройки БД, строки подключения для бэкенда).
        - `depends_on`: для определения зависимостей и порядка запуска сервисов (например, бэкенд зависит от БД).
        - `networks`: для подключения сервисов к пользовательским сетям, обеспечивая их изоляцию и взаимодействие.
    - `networks` (верхнеуровневый): для определения кастомных сетей.
    - `volumes` (верхнеуровневый): для определения именованных томов, обеспечивающих персистентное хранение данных.
- Освоил основные команды Docker Compose:
    - `docker-compose up [-d] [--build]`: для запуска всех сервисов (в фоновом режиме, с пересборкой образов при необходимости).
    - `docker-compose down [-v]`: для остановки и удаления контейнеров, сетей (и томов с флагом `-v`).
    - `docker-compose ps`: для просмотра статуса сервисов.
    - `docker-compose logs [service_name]`: для просмотра логов (всех сервисов или конкретного).
    - `docker-compose build [service_name]`: для сборки или пересборки образов.
    - `docker-compose exec <service_name> <command>`: для выполнения команд внутри контейнера запущенного сервиса.
- Понял, как Docker Compose упрощает оркестровку локального окружения разработки и тестирования, позволяя одной командой поднять весь стек разрабатываемого приложения.

🚀 **Вывод для текущего проекта и общих навыков:** Docker Compose – это ключевой инструмент, который связывает воедино все компоненты разрабатываемого многокомпонентного приложения (например, фронтенд, бэкенд, база данных) и делает управление ими значительно проще. Это знание окончательно проясняет, как достигается простота развертывания сложных систем.
- **Как системный архитектор:** Понимание Docker Compose позволяет мне проектировать всю систему приложения как единое целое, описывая все сервисы, их конфигурации, зависимости и сетевые взаимодействия в декларативном `docker-compose.yml` файле. Это обеспечивает воспроизводимость и консистентность окружения от разработки до (потенциально) продакшена. Я могу четко определить, как бэкенд-сервис подключается к сервису базы данных, как фронтенд-сервис обращается к бэкенду, и как данные базы данных сохраняются между перезапусками с помощью именованных томов. Это реализует на практике решение о контейнеризации (которое было принято ранее, например, 18.04-19.04) и объединяет знания о `docker run` (полученные, к примеру, 01.05-02.05) в управляемую систему.
- **Как DevOps-инженер:** `docker-compose.yml` становится стандартом де-факто для локальной разработки в команде. Это позволяет каждому разработчику быстро поднять весь стек приложения (`docker-compose up`) и начать работу, не тратя время на ручную настройку каждого компонента и их связей. Я могу использовать Docker Compose для создания окружений для автоматизированного интеграционного тестирования в CI/CD пайплайнах. Управление всем приложением (запуск, остановка, просмотр логов, обновление образов) становится значительно проще, что повышает эффективность и снижает вероятность ошибок при развертывании и поддержке. Это знание объясняет, как можно эффективно организовать деплой (как это было сделано 30.04-02.05), и дает инструменты для его дальнейшей поддержки и эволюции.

---

## **16.05.2025 – 19.05.2025**

🔹 **Фокус:** Создание и использование Docker-репозиториев (реестров) для хранения и распространения образов, на основе статьи Selectel "Как создать свой Docker-репозиторий".
- Углубился в понятие Docker-репозитория (или реестра) как централизованного хранилища для Docker-образов. Понял, что это ключевой компонент для управления жизненным циклом образов.
- Изучил различные типы реестров:
    - **Публичные реестры**, такие как Docker Hub, удобные для общедоступных образов и быстрого старта.
    - **Приватные реестры**, предоставляемые облачными провайдерами (например, Selectel Container Registry, AWS ECR, Google GCR, Azure ACR) или разворачиваемые самостоятельно (например, с помощью Docker Registry или Harbor). Понял их преимущества в плане безопасности, контроля доступа и производительности для корпоративных нужд.
- Разобрал основные операции для работы с реестрами:
    - `docker login [SERVER]`: для аутентификации на сервере реестра.
    - `docker tag SOURCE_IMAGE[:TAG] TARGET_IMAGE[:TAG]`: для присвоения образу нового имени (тега), которое включает адрес реестра, имя репозитория и версию (например, `cr.selectel.com/my-namespace/my-app:v1.0.0`). Это критически важный шаг перед отправкой образа в удаленный реестр.
    - `docker push TARGET_IMAGE[:TAG]`: для загрузки образа в реестр.
    - `docker pull TARGET_IMAGE[:TAG]`: для скачивания образа из реестра на локальную машину или сервер.
- Осознал важность правильной организации именования образов и версионирования с помощью тегов для обеспечения порядка и возможности отката к предыдущим версиям.
- Проанализировал, как выбор реестра (публичный/приватный, облачный/локальный) влияет на процессы разработки и развертывания.

🚀 **Вывод для текущего проекта и общих навыков:** Понимание работы с Docker-репозиториями необходимо для организации эффективного процесса сборки и развертывания любого контейнеризированного приложения.
- **Как системный архитектор:** Это знание позволяет мне принимать обоснованные решения о том, где и как будут храниться Docker-образы для разрабатываемой системы. Я могу выбрать оптимальный тип реестра (например, приватный облачный реестр для лучшего контроля и интеграции с инфраструктурой), спроектировать стратегию именования и тегирования образов, которая обеспечит прозрачность версионирования и облегчит управление зависимостями между различными сервисами (например, фронтендом, бэкендом, базой данных). Это напрямую влияет на безопасность, доступность и управляемость артефактов сборки.
- **Как DevOps-инженер:** Навыки работы с Docker-репозиториями являются основой для построения CI/CD пайплайнов. Я смогу автоматизировать процесс сборки образов, их корректного тегирования (например, с использованием версий из Git или номеров сборок) и последующей загрузки (`docker push`) в выбранный реестр. Также я смогу настроить окружения (staging, production) на скачивание (`docker pull`) нужных версий образов из реестра во время развертывания. Это обеспечивает консистентность между сборкой и деплоем, позволяет быстро откатывать изменения и управлять артефактами на протяжении всего их жизненного цикла. Понимание `docker login` важно для безопасной аутентификации CI/CD агентов в реестре.

---

### Этот путь, начиная от проектирования БД и заканчивая паттернами проектирования и Docker, заложил прочный фундамент для моей роли DevOps-инженера и системного архитектора в проекте. Каждое изученное понятие напрямую или косвенно способствует созданию надежной, масштабируемой и легко поддерживаемой системы. Теперь я лучше понимаю, как обеспечить стабильную работу PostgreSQL, как спроектировать эффективные API на ASP.NET Core, как упаковать все в Docker-контейнеры и как выбрать правильные архитектурные решения для долгосрочного успеха проекта.
